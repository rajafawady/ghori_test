# Environment Configuration for CV Matcher Database Tests

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cv-matcher
DB_USER=postgres
DB_PASSWORD=6731

# Test Database (optional - will use main DB if not specified)
TEST_DB_NAME=cv-matcher-test
TEST_DB_USER=postgres
TEST_DB_PASSWORD=6731

# Test Configuration
NODE_ENV=test
TEST_TIMEOUT=30000

# Logging
LOG_LEVEL=error
